server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://revplay-db.c564s4oe2c1u.ap-south-1.rds.amazonaws.com:3306/revplay
    username: ${AWS_USERNAME}
    password: ${AWS_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

  mail:
    host: smtp.gmail.com
    port: 587
    username: balusuharshasri2807@gmail.com
    password: ucoovonyjqqegato
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

app:
  security:
    jwt:
      secret: ${secret_key:}
      access-token-expiration-seconds: 3600
      refresh-token-expiration-seconds: 1209600

revplay:
  upload:
    base-dir: uploads
    songs-dir: songs
    podcasts-dir: podcasts
    images-dir: images
    ads-dir: ads

logging:
  level:
    com.revplay: INFO

file:
  storage:
    type: s3

ai:
  provider: grok
  api-key: ${XAI_API_KEY:}
  url: https://api.x.ai/v1/chat/completions

aws:
  accessKey: ${aws_access_key}
  secretKey: ${aws_secret_key}
  region: ap-south-1
  s3:
    bucket: revplay-media-harshasri

